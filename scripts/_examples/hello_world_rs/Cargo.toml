[package]
name = "hello_world_rs"
version = "1.0.0"
edition = "2021"

# Keep this example out of the main workspace so it can be built independently.
[workspace]

[[bin]]
name = "hello_world_rs"
path = "src/main.rs"

[dependencies]
agrr-script-sdk = { path = "../../../sdk/rust" }
serde_json = "1"
